apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Values.green.name }} # Switch to blue for rollback
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080